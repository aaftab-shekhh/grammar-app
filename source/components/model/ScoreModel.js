import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ScoreModel = () => {
  return (
    <View>
      <Text>ScoreModel</Text>
    </View>
  );
};

export default ScoreModel;

const styles = StyleSheet.create({});
